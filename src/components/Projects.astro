---
const projects = [
  {
    id: 1,
    title: "Care Pet",
     
   description:"Created Care Pet, a user-friendly website for pet adoption and donation, using PERN stack.",
  
    links: [
      { title: "Click here", url: "https://github.com/Jemipaul123/Pet_adoption_website.git" },
      
    ],
    technologies: ["React", "Nodejs","Express js","PostgreSQL"]
  },
  {
    id: 2,
    title: "Fix It",
    description: "Developed Fix It, an innovative local job allocation app that allocates jobs to local workers and also allowing users to book the local job services using Flutter and Firebase.",
    completed: true,
    links: [{title: "Click here", url:"https://github.com/Jemipaul123/Fix_it"}],
    technologies: ["Flutter"," Firebase"]
  },
   {
    id: 3,
    title: "Hola App",
    description: "A social media application with tweeting functionality, Hola App was developed using Flutter and Firebase.",
   
    links: [{title: "Click here", url:"https://github.com/Jemipaul123/Hola_-App.git"}
    ],
    technologies: ["Flutter", "Firebase"]
  },
   {
    id: 4,
    title: "Portfolio Website",
    
    description: "A portfolio website made with Astro js",
   
    links: [],
    technologies: ["Astro"]
  },
   
  // Add more projects as needed
];
---

<section class="projects-section">
  <h2>Projects</h2>
  <div class="projects-grid">
    {projects.map((project) => (
      <div class="project-card" key={project.id}>
        <h3>{project.title}</h3>
        <p class="description">{project.description}</p>
        <p class="content">{project.content}</p>
        <p class="technologies">
          <strong>Technologies:</strong> {project.technologies.join(", ")}
        </p>
        {project.links.length > 0 && (
          <div class="links">
            {project.links.map((link) => (
              <a href={link.url} target="_blank" rel="noopener noreferrer" class="link">
                {link.title}
              </a>
            ))}
          </div>
        )}
       
      </div>
    ))}
  </div>
</section>

<style>
  .projects-section {
    padding: 2rem;
    background-color: var(--accent-overlay);
    border-radius: 1rem;
    margin-top: 2rem;
  }

  .projects-section h2 {
    text-align: center;
    margin-bottom: 1.5rem;
    font-size: 2rem;
    color: #8f34bc;
  }

  .projects-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 1.5rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .project-card {
    background-color: #ffffff;
    border: 1px solid var(--accent-light);
    border-radius: 0.5rem;
    padding: 1rem;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
  }

  .project-card:hover {
    transform: translateY(-0.5rem);
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
  }

  .project-card h3 {
    margin: 0 0 0.5rem 0;
    font-size: 1.5rem;
    color: var(--accent-regular);
  }

  .project-card .description {
    margin: 0.5rem 0;
    font-size: 1rem;
    color: var(--accent-light);
  }

  .project-card .content {
    margin: 0.5rem 0;
    font-size: 0.9rem;
    color: var(--accent-light);
  }

  .project-card .technologies {
    margin: 0.5rem 0;
    font-size: 0.9rem;
    color: var(--accent-regular);
  }

  .project-card .links a {
    display: inline-block;
    margin-right: 0.5rem;
    font-weight: bold;
    color: var(--accent-light);
  }

  .project-card .last-updated {
    margin-top: 1rem;
    font-size: 0.8rem;
    color: var(--accent-light);
    text-align: right;
  }
</style>
